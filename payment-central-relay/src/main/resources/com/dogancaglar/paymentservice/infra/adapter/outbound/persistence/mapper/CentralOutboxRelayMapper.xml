<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dogancaglar.paymentservice.infra.adapter.outbound.persistence.mapper.CentralOutboxRelayMapper">

    <resultMap id="CentralOutboxResultMap" type="com.dogancaglar.common.db.entity.OutboxEventEntity">
        <id property="oeid" column="oeid"/>
        <result property="partitionKey" column="partition_key"/>
        <result property="eventType" column="event_type"/>
        <result property="aggregateId" column="aggregate_id"/>
        <result property="eventId" column="event_id"/>
        <result property="parentEventId" column="parent_event_id"/>
        <result property="payload" column="payload"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"
                typeHandler="com.dogancaglar.common.db.typehandler.InstantTypeHandler"/>
        <result property="updatedAt" column="updated_at"
                typeHandler="com.dogancaglar.common.db.typehandler.InstantTypeHandler"/>
        <result property="claimedAt" column="claimed_at"
                typeHandler="com.dogancaglar.common.db.typehandler.InstantTypeHandler"/>
        <result property="claimedBy" column="claimed_by"/>
    </resultMap>

    <select id="findEligible" resultMap="CentralOutboxResultMap">
        WITH claimed AS (
        UPDATE outbox_event
        SET status = 'PROCESSING',
        claimed_at = (now() AT TIME ZONE 'UTC'),
        claimed_by = #{workerId}
        WHERE (oeid, created_at) IN (
        SELECT oeid, created_at
        FROM outbox_event
        WHERE status = 'NEW'
        AND created_at &lt;= #{tSafe, typeHandler=com.dogancaglar.common.db.typehandler.InstantTypeHandler}
        ORDER BY created_at
        LIMIT #{batchSize}
        FOR UPDATE SKIP LOCKED
        )
        RETURNING *
        )
        SELECT * FROM claimed
    </select>



    <update id="reclaimStuckClaims" parameterType="int">
        UPDATE outbox_event
        SET status = 'NEW',
        claimed_at = NULL,
        claimed_by = NULL
        WHERE status = 'PROCESSING'
        AND claimed_at &lt; ((now() AT TIME ZONE 'UTC') - (#{olderThanSeconds} || ' seconds')::interval)
    </update>


    <select id="countEligible" resultType="long">
        SELECT COUNT(*) FROM (
            SELECT 1 FROM outbox_event
            WHERE status = 'NEW'
              AND created_at &lt;= #{tSafe, typeHandler=com.dogancaglar.common.db.typehandler.InstantTypeHandler}
            LIMIT 10000
        ) sub
    </select>



    <update id="markDispatched">
        UPDATE outbox_event
        SET status = 'SENT',
        updated_at = now() AT TIME ZONE 'UTC',
        claimed_at = NULL,
        claimed_by = NULL
        WHERE oeid = #{oeid}
        AND created_at = #{createdAt, typeHandler=com.dogancaglar.common.db.typehandler.InstantTypeHandler}
    </update>


    <update id="unclaimSpecific">
        UPDATE outbox_event
        SET status = 'NEW',
        claimed_at = NULL,
        claimed_by = NULL,
        updated_at = now() AT TIME ZONE 'UTC'
        WHERE oeid = #{oeid}
        AND created_at = #{createdAt, typeHandler=com.dogancaglar.common.db.typehandler.InstantTypeHandler}
        AND claimed_by = #{workerId}
    </update>

    <!-- Edge Watermark Operations -->

    <select id="computeTSafe" resultType="java.time.Instant">
        SELECT MIN(forwarded_up_to) FROM edge_watermarks
    </select>

    <delete id="deleteWatermark" parameterType="string">
        DELETE FROM edge_watermarks WHERE edge_node_id = #{edgeNodeId}
    </delete>


</mapper>
